server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/krixon/spring-config.git
          search-paths:
            - '*'
  security:
    user:
      name: user
      password: password
      roles:
        - USER